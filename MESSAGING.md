# OIOXO Messaging — plain-words source of truth

> One place that decides how we explain OIOXO everywhere: the site, the README, the
> Marketplace, the docs, and in-app help. Rule #1: **a grandmother should understand it.**
> Say what it *does for you* first; the technical name (if any) comes second and small.
> Grounded in `oioxoai/platform/05_CONTENT_VOICE.md` and the real code — don't drift.

---

## What OIOXO is (one line)

**OIOXO is a private AI that builds real apps on your own device — and saves you money on AI.**

Longer: *Describe an app in a sentence and OIOXO writes it, runs it, and fixes it until it
works — all on your own computer or phone. Your code never leaves your device. It's free to
start, and it cuts your AI costs by about 90%.*

Don't call it "a chatbot" or just "an AI IDE." It's broader: **chat, edit, create, and code** —
all on your device.

---

## The three winners (lead with these — in plain words)

### 1. Your code stays private — it never leaves your device
Plain: *The AI runs on your own machine, not on our servers. So the code you write and the
things you ask never get uploaded. Privacy isn't a setting you turn on — it's just how it's
built.*
Proof words: "Your code never leaves your device." · "Nothing touches our servers." · only the
*count* of usage is measured, never your code.

### 2. It saves you about 90% on AI — even in the tools you already use
Plain: *AI tools charge you per word. OIOXO sends the AI only the small slice of your project
it actually needs, instead of the whole thing — so you pay for a fraction of the words and get
the same answer. A question that would cost ~50,000 words comes back as ~5,000.*
**The buried part to make loud:** this works **inside the editors you already use** — GitHub
Copilot, Cursor, Claude, Windsurf, and more — not only inside OIOXO. One small install and your
existing AI bill drops ~90%. (Feature name, secondary: "the context engine.")
Honest numbers (don't inflate): **~90% fewer tokens** (measured), **10–20× smaller** context.

### 3. Your devices team up to build together  ← the hidden winner, make it shine
Plain (grandmother test): ***Put your phone and your laptop on the same Wi-Fi and they work on
the same project together. The weaker one borrows the stronger one's power — so your old phone
can build like a fast laptop. And the time your devices spend helping each other adds free
build time to your account.***
How to say the details simply:
- It's **your own devices**, signed into your account, on the **same Wi-Fi**. (Not a stranger's.)
- You start it with one button: **"Add a device"** — show a code on one, scan it on the other.
- Nothing goes through us — the devices talk to each other directly.
- The helping device can stop anytime; your project always lives safely on the main device.

**Headline rule:** the title explains it in plain words ("Your devices team up to build
together" / "Add a device"). The name **"Compute Mesh"** may appear *underneath, small* — never
as the thing that has to carry the explanation.

---

## The rest of the story (supporting, not buried)

- **Real apps, not snippets.** It plans → writes → **runs** → repairs until the app actually
  works. The check mark means it *ran*, not that it looked right.
- **Everywhere you build.** One account across web, desktop (Win/Mac/Linux), phone (iOS/Android),
  the terminal, and inside VS Code.
- **Use your own AI key if you want.** Bring a key from 12+ providers (or run a local model with
  Ollama) — free for everyone, and OIOXO still trims the bill ~90%.
- **Free to run.** Using the AI models is free; you only pay for *more time*. Pro is **$3.99/mo**
  (or $39/yr) and just removes the wait — model access stays free either way.

---

## Words to use / avoid

**Use:** plain verbs, short sentences, real numbers. "Runs on your device." "Your code stays
yours." "Saves ~90%." "Add a device." "Build it."

**Avoid:**
- Jargon as a headline: "Compute Mesh," "context engine," "MCP," "WebGPU," "capsule" — fine as a
  small secondary label, never as the explanation.
- Hype: no "🚀", no "supercharge," no "AI magic," no exclamation marks in product UI.
- Naming the internal model (no "Qwen", no sizes). Users see tiers (Basic / Light / Max).
- "A chatbot" / "just an AI IDE" — undersells it.
- Implying you must pay to unlock a model. (You don't — only *time* is metered.)

OIOXO is always **UPPERCASE** in copy (except the lowercase wordmark logo + domain/package ids).
